# Cloudflare Pages redirects for migrare.creadev.org
#
# Trailing slash handling: intentionally EMPTY.
# CF Pages Pretty URLs feature handles /app/ → /app automatically.
# Adding redirect rules for trailing slashes creates an infinite redirect
# loop against CF Pages' own normalization. Leave it to CF.
#
# SPA fallback (/* /index.html 200) is also absent — Prestruct prerendering
# means CF Pages serves the correct index.html directly per route.
